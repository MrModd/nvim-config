--------------------------------
-- Generic Vim configurations --
--------------------------------

-- Vim configuration kept in vimrc format to make at least
-- the basic customizations compatible with vim
vim.cmd("source " .. vim.fn.stdpath("config") .. "/vimrc")
